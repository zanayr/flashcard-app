import React from 'react';


const withContextActions = (WrappedComponent) => {
    return (
        <WrappedComponent>
            
        </WrappedComponent>
    );
}


export default withContextActions;