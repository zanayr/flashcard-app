import React, {Component} from 'react';

import '../../../style.css';
import CSS from './Main.module.css';

class Main extends Component {
    render() {
        return (
            <main className={CSS.main_default}>
                
            </main>
        )
    }
}

export default Main;