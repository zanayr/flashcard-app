import React from 'react';

import CollectionInspector from './Collection/CollectionInspector';

const inspector = (props) => {
    return (
        <div>
            <CollectionInspector/>
        </div>
    );
}

export default inspector;