import React from 'react';

import GlobalCSS from '../../../Global.module.css';
import SpaceCSS from './Space.module.css';

const row = (props) => {
    return (
        <span className={SpaceCSS.Horizontal_Space}>
        </span>
    );
}

export default row;